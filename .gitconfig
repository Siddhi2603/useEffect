[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	name = Siddhi Sonare
	email = ssiddhisonare26@gmail.com
[credential]
	helper = wincred
